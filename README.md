# Vector

This project hardens the build in `Vec` struct with additional metadata.

This can be used in when indexing a `Vec` at an age and let the struct handle the index shenanigans.

## TODOs

- [ ] Add tests